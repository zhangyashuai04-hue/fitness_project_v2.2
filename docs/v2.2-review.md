# v2.2 独立审查

审查发现5项问题，均已修复并运行回归测试：
- 校验失败后的待处理命令不再锁住训练。
- 空动作名不会偷偷沿用不可见旧名称保存。
- 原始名称、次数与重量输入持久化，非法草稿暂停恢复后保留。
- 未编辑的体重失焦不改写原精度或单位。
- 系统时间回拨时冻结已观察到的计时并提示。

完整测试158项通过（含提示显示和构建版本门槛）。独立审查不替代APK/设备验证。

实施决策：使用用户指定独立目录而非嵌套worktree；新界面复用旧命令控制器但独立文件；新入口restore_free显式转换旧会话；不生成无用模板/计划行；改名写权威会话/组元数据，旧gym_sets镜像名不改，以保留其旧外键引用。因而降级旧界面可能显示改名前名称，本版本展示新名称。

JDK只读复用已有工具链；新缓存/产物在v2.2目录。独立审查在构建期间进行，以便修复后只需一次最终重打包。
