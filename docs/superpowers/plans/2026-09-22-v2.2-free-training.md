# v2.2 自由训练 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在独立v2.2目录复用Python版本，实现用户确认的自由训练、按日只读记录和体重输入，并兼容Dart/Python旧数据。

**Architecture:** 继续使用Flet界面、Python服务及SQLite。采用追加迁移，复用事务、幂等命令、备份和签名构建；自由训练增加独立入口，旧表和旧记录保留。先完成存储和训练规则，再接界面，最后验证升级并导出APK。

**Tech Stack:** Python >=3.12、Flet 0.86.5、flet-charts 0.86.5、SQLite、pytest、现有Android双ABI构建工具链。

**Spec:** `docs/superpowers/specs/2026-09-22-v2.2-free-training-design.md`（已确认）。

## Global Constraints

- 全部新增源码、虚拟环境、测试、文档和产物在 `D:/zys/myself/codex/fitness_project_v2.2`；基线 `D:/zys/myself/codex/fitness_project_v2.1` 只读。
- 底部顺序“记录 / 训练 / 我的”，默认记录；草图仅用于结构，颜色沿用统一主题。
- 取消模板、日期计划、饮食入口、历史修改和手动补录；数据库旧表保留。
- 下一组要求次数、重量齐全；下一动作/结束二次确认，任一项有效即保存，两项都空不保存。
- 次数正整数，重量有限非负数；允许0kg，缺失不是0；保存组须有动作名。
- 包名 `com.presley.flexify.localflow`、原签名、`app_flutter/flexify.sqlite` 保持兼容；支持v58、v59及新增v60备份。
- 新建独立 `.venv`，不复制旧虚拟环境、缓存、用户数据库或整个旧Git目录。
- 不增加账号、服务器、云同步；iOS不在本次范围。
- 当前基线已使用versionName 2.2.0，不能靠版本名判断覆盖升级。拟BuildNumber40709，实际APK每个ABI的versionCode必须高于已分发版本。
- 每个任务只提交本任务文件；不使用全目录git add；不修改旧项目。

## Review Focus

1. v58/v59迁移中断及不支持的库：保留原库，不产生空库或丢记录（任务1、7）。
2. 单项输入、0、空白、NaN/Infinity及快速重复提交：正确区分有效/缺失，组不重复（任务2、3）。
3. 确认弹窗期间退出、暂停后重启、跨午夜和系统时间倒退：草稿恢复、无负计时/重复计时（任务4）。
4. 旧awaiting_choice及纯计时会话：已保存组不再保存，旧时长不转成次数（任务4、7）。
5. 同名动作跨会话、翻看过去日期后输入体重、旧体重单位：归属不混淆，不偷偷修改历史（任务5、6）。

## 文件和依赖地图

以下相对路径均以v2.2为根。旧项目存在的文件先按白名单复制；“修改”均指新目录中的副本。

- `app/src/fitness/storage/migrations.py`：v60追加迁移与备份；`storage/legacy.py`：旧活动状态适配。
- `app/src/fitness/domain/models.py`：扩展Snapshot；`domain/free_training.py`：输入规则；`domain/training_time.py`：活动区间按日计时。
- `app/src/fitness/services/training.py`：自由会话、改名、原子推进和命令重试。
- `app/src/fitness/services/records.py`：按日期只读视图、本周次数；原weight/trends/backup服务继续复用。
- `app/src/fitness/ui/training.py`：四按钮和恢复；`ui/records.py`：记录、今日体重及唯一图表；原settings/forms/theme继续复用。
- `app/src/main.py`及现有AppServices定义所在文件：三页导航和依赖注入。执行时先用 `rg -n 'class AppServices' app/src` 定位，不另建重复容器。
- `tests/test_v60_migration.py`、`test_free_measurements.py`、`test_free_training.py`、`test_training_recovery_v22.py`、`test_records_v22.py`、`test_ui_v22.py`、`test_upgrade_v22.py`：对应边界测试。
- `scripts/build-app.ps1`、`toolchain.ps1`、`verify-release.py`：沿用构建、提高版本、校验身份；`deliverables/安装与旧数据继承.md`：交付说明。

依赖顺序：1 → 2 → 3 → 4 → 5 → 6 → 7 → 8。无需并行修改同一训练服务。

## 公共接口约定

保留现有Measurements(weight,reps,duration_seconds)。新输入只写weight/reps，旧duration_seconds仍可读。
扩展现有Snapshot，不删除旧字段；新增 `action_name: str`、`action_elapsed_ms: int`、`daily_elapsed_ms: int`、`can_inherit: bool`。新字段给兼容默认值，旧命令结果可反序列化。
所有训练变更返回Snapshot。`Clock.now_ms() -> int`，`Clock.today() -> datetime.date`沿用现有实现/测试FakeClock。

训练新增/改造接口：
```python
# TrainingService(db: sqlite3.Connection, clock: Clock)
start_free() -> Snapshot
rename_current(session_id: str, name: str) -> Snapshot
save_draft(session_id: str, measurements: Measurements) -> Snapshot
advance(session_id: str, action: Literal['next_set','next_action','end'],
        expected_revision: int, command_id: str) -> Snapshot
begin_confirmation(session_id: str, action: Literal['next_action','end']) -> Snapshot
cancel_confirmation(session_id: str) -> Snapshot
get(session_id: str) -> Snapshot
restore_active() -> Snapshot | None
inherit(session_id: str) -> Snapshot
pause(session_id: str) -> Snapshot
resume(session_id: str) -> Snapshot
```
已有数据库事务与py_commands重用，不另造事件框架。旧complete/choose/start等接口先保留兼容测试；新UI只调用以上入口。

测试命令统一在v2.2根目录运行：`$env:PYTHONPATH = "$PWD/app/src"`，随后 `& .\.venv\Scripts\python.exe -m pytest <文件> -q`。预期红灯必须是指定功能缺失/断言失败，不接受依赖安装失败作为红灯。

### Task 1: 可运行的独立副本与v60安全迁移

**Files:** 复制 `app/src/`、`app/pyproject.toml`、现有测试及合成fixtures、构建scripts、许可证；修改 `storage/migrations.py`；新增 `tests/test_v60_migration.py`、`.gitignore`。
**Interfaces:** Consumes现有 `initialize(path: Path, backup_dir: Path)` 和 `open_database(path)`；Produces同接口支持58/59/60，新库60，旧业务表值不变。

- [ ] 按白名单复制源码，排除__pycache__/build/cache/用户库；独立环境安装。复制前确认目标不存在同名用户改动；存在则逐文件比较。
```powershell
& 'D:\python\安装位置\python.exe' -m venv .venv
& .\.venv\Scripts\python.exe -m pip install -e .\app pytest
```
- [ ] 在新目录初始化Git并记录可复现基线；`.gitignore`排除 `.venv/`、`.cache/`、`app/build/`、`research/`、`deliverables/`、`__pycache__/`。旧Git不复制。
- [ ] 添加迁移红灯测试。复用现有legacy_db合成fixture；`sqlite3`/initialize在测试文件正常导入。
```python
def test_v58_to_v60_is_repeatable(legacy_db, tmp_path):
    initialize(legacy_db, tmp_path/'backups')
    initialize(legacy_db, tmp_path/'backups')
    with sqlite3.connect(legacy_db) as db:
        assert db.execute('PRAGMA user_version').fetchone()[0] == 60
```
- [ ] 运行 `tests/test_v60_migration.py`，预期当前版本59断言失败。
- [ ] 在已有备份/事务迁移中追加60，运行状态写 `runtime_version=2`；不改写旧业务表，按启动时旧版本各备份一次。版本未知抛错。迁移记录写py_migrations，不重复插入。
```python
if version not in (58, 59, 60):
    raise ValueError('不支持的数据库版本')
# 58先执行已有59迁移；59追加60。均在原initialize事务中。
```
- [ ] 增加故障注入：执行v60最后写入前抛sqlite3.OperationalError，重新打开原库断言版本和业务表内容与迁移前一致；新建库失败仅移除本次创建的文件。运行本任务及原storage/backup测试，全部通过。
- [ ] 显式暂存本任务文件并提交 `feat: bootstrap v22 with safe v60 migration`。

### Task 2: 部分记录与严格输入规则

**Files:** 新增 `domain/free_training.py`、`tests/test_free_measurements.py`；修改 `domain/models.py`。
**Interfaces:** Consumes Measurements；Produces `parse_measurements(reps: str, weight: str) -> Measurements`、`should_save(value: Measurements) -> bool`、`validate_next_set(value: Measurements) -> None`。非法输入raise ValueError，缺失保持None。

- [ ] 添加测试：
```python
@pytest.mark.parametrize('reps,weight,save', [('', '', False),('10','',True),('','0',True),('10','40',True)])
def test_partial_values(reps, weight, save):
    value = parse_measurements(reps, weight)
    assert should_save(value) is save
    if weight == '':
        assert value.weight is None
```
- [ ] 运行 `tests/test_free_measurements.py`，预期新函数尚不存在。
- [ ] 实现空白归一、正整数字符串校验及math.isfinite重量校验；重量负数报错。下一组检查两个字段非None；不要使用真假值判断0。核心保存条件：
```python
def should_save(value):
    return value.reps is not None or value.weight is not None
```
- [ ] 加入 `nan/inf/-1/1.5次/非数字/仅空格/0kg` 参数测试，以及不完整下一组raise测试。运行本任务测试通过。
- [ ] 扩展Snapshot及其序列化默认值，验证旧snapshot命令JSON可读。提交 `feat: define partial set and numeric input rules`。

### Task 3: 无模板训练及原子推进

**Files:** 修改 `services/training.py`、Snapshot构造处；新增 `tests/test_free_training.py`。
**Interfaces:** Consumes任务2函数和原transaction/db；Produces公共约定中的start_free/rename_current/advance/inherit。动作身份用独立slot_id，不按名字合并。

- [ ] 增加 `free_service` fixture：用tmp_path新库initialize/open_database与现有clock构建TrainingService，不使用含旧活动会话的db fixture；yield后关闭连接。
- [ ] 添加首个行为测试：
```python
def test_partial_set_can_end(free_service):
    s = free_service.start_free()
    s = free_service.rename_current(s.id, '深蹲')
    s = free_service.save_draft(s.id, Measurements(reps=8))
    s = free_service.advance(s.id, 'end', s.revision, 'end-1')
    rows = free_service.db.execute('SELECT measurements FROM training_set_results WHERE session_id=?', (s.id,)).fetchall()
    assert len(rows) == 1
    assert json.loads(rows[0][0])['reps'] == 8
```
- [ ] 运行 `tests/test_free_training.py`，预期缺start_free。实现无模板会话：原plan_id约束需要关联时建立隐藏内部计划，不创建可见模板；唯一active_slot保护第二次开始返回原会话。
- [ ] 实现advance使用现有_command事务，先验证再写组/legacy镜像再推进，末尾存命令结果。next_set强制完整；其他两种should_save为真才写组。下一动作新slot，组1、名称/草稿清空。空动作连续切换不生成历史组。
```python
if action == 'next_set':
    validate_next_set(draft)
if should_save(draft) and not action_name.strip():
    raise ValueError('请输入动作名称')
```
- [ ] 继承按session_id+slot_id取上一已存组；改名同步当前slot对应meta名称/旧镜像，不更新其他session。无上组返回原空草稿。
- [ ] 参数化测试两种离开动作×四种空值组合、完整下一组、0kg、跨动作继承、改名归属、20组连续添加；同command_id调用两次仅一组；中途SQL异常回滚不推进。运行本任务与原幂等测试通过。
- [ ] 提交 `feat: add atomic free training transitions`。

### Task 4: 计时、确认和旧活动恢复

**Files:** 修改 `services/training.py`、`storage/legacy.py`；新增 `domain/training_time.py`、`tests/test_training_recovery_v22.py`。
**Interfaces:** ConsumesClock、Snapshot；Producesbegin_confirmation/cancel_confirmation和 `daily_duration(intervals: list[tuple[int,int]], day: date) -> int`。区间是本地时间对应的Unix毫秒，使用当地午夜界限分割。

- [ ] 添加确认暂停测试：
```python
def test_confirmation_excludes_wait(free_service, clock):
    s = free_service.start_free()
    clock.advance_ms(2000)
    before = free_service.begin_confirmation(s.id, 'end')
    clock.advance_ms(5000)
    after = free_service.cancel_confirmation(s.id)
    assert after.set_elapsed_ms == before.set_elapsed_ms
```
- [ ] 运行本任务测试红灯，随后将动作/组起点、累计、活动区间及confirmation前状态持久化至runtime_json。确认期间冻结；取消恢复原running或paused。确认成功由advance消费且清除pending状态。
- [ ] 计时区间交集只累加非负时长：
```python
contribution = max(0, min(end_ms, day_end_ms) - max(start_ms, day_start_ms))
```
时钟倒退冻结该段并设置notice；每秒UI刷新不调用_store。跨日统计合并已关闭区间与当前运行段；旧未知区间不能编造，保留旧累计并说明粒度不足。
- [ ] 恢复旧awaiting_choice标记已保存，草稿不带入二次保存；旧纯计时当前动作保留历史，切新自由slot并显示一次提示；停止旧未来动作自动推进。
- [ ] 测试取消/确认各自running与paused、确认中重建服务恢复草稿、后台继续、暂停重启不涨时间、23:59:59跨日分段、时钟倒退；构建59 awaiting_choice及timed夹具验证不重复记录。运行本任务及任务3全部通过。
- [ ] 提交 `feat: persist training timing and upgrade active sessions`。

### Task 5: 日期记录与体重统计

**Files:** 新增 `services/records.py`、`tests/test_records_v22.py`；复用 `services/weight.py`、`services/trends.py`。
**Interfaces:** `RecordsService(db, clock)`；`day_sessions(day: date) -> list[dict]`，每项含session_id、actions；action含slot_id、name、sets；set含reps、weight、duration_seconds且允许None。`week_count(day: date) -> int`；`format_set(measurements: Measurements) -> str`。

- [ ] 添加格式测试：
```python
def test_missing_weight_is_not_zero():
    assert format_set(Measurements(reps=8)) == '8次 × —'
    assert format_set(Measurements(reps=8, weight=0)) == '8次 × 0kg'
```
- [ ] 运行本任务红灯，实现按session+slot/exercise_index分组；数值读training_set_results而非legacy镜像默认0。旧timed输出原时长/单位，未知时长显示缺失。
- [ ] week_count按周一边界筛选已结束且EXISTS组的会话，不能COUNT DISTINCT日期。体重图复用现有kg/lb/stone换算，记录查询无写入。
- [ ] 测试同日两次=2、空训练=0、未结束=0、周日/周一边界、同名不同slot不合并、旧单位/时长、部分组计组数。运行本任务及原weight测试通过。
- [ ] 提交 `feat: add read-only daily records and weekly session counts`。

### Task 6: 三页UI与四按钮交互

**Files:** 修改 `ui/training.py`、`app/src/main.py`、AppServices现有定义文件、settings；新增 `ui/records.py`、`tests/test_ui_v22.py`。
**Interfaces:** TrainingController新增 `start_free()`、`rename_current(name: str)`、`advance(action: str)`、`begin_confirmation(action: str)`、`cancel_confirmation()`；复用update_inputs/inherit/pause/resume。RecordsController新增 `select_day(day: date)`、`save_today_weight(value: float)`，选择日期存selected_day；体重保存始终使用clock.today()。

- [ ] 添加控制器测试：用FakeClock及WeightService/RecordsService构建RecordsController，选择昨天后save_today_weight(75)，断言今天75、昨天无新增；训练controller取消确认不得调用advance。
- [ ] 运行 `tests/test_ui_v22.py` 红灯；实现RecordsController与训练控制器适配。输入变化先保存草稿，命令按钮禁用至请求完成；失败恢复可重试，复用同command_id。
- [ ] 导航固定顺序：
```python
labels = ['记录', '训练', '我的']
selected_index = 0
```
记录页显示日期、周次数、逐日明细、前后按钮/手势、今日体重输入和唯一体重图；无历史编辑/补录控件。
- [ ] 训练页实现名称、两项输入、三种计时、2×2按钮和暂停。两种确认文案严格为“是否切换动作？”和“是否结束训练？”。返回键/遮罩关闭走取消逻辑，不遗留冻结状态。
- [ ] 新界面不引用模板/饮食/历史写服务；设置页保留备份、显示、本地说明和许可证。恢复成功后重建所有服务和controller，旧连接不再被刷新任务引用。
- [ ] 测试导航默认值、四按钮状态、首组继承禁用、部分输入只影响next_set、弹窗返回取消、单个定时刷新任务卸载取消。运行本任务通过；模拟器人工检查键盘遮挡、小屏滚动及安全区留到任务8。
- [ ] 提交 `feat: simplify app to records training and settings`。

### Task 7: 两代数据升级与恢复回归

**Files:** 新增 `tests/test_upgrade_v22.py`、合成 `tests/fixtures/python_v59.sql`；必要时修改现有backup适配版本列表。
**Interfaces:** Consumes initialize/BackupService.export(target)/restore(source)、TrainingService.restore_active/RecordsService.day_sessions；Produces58/59/60恢复报告。真实用户库不进Git。

- [ ] 添加参数化升级测试，用原v58合成fixture及由基线v2.1生成的v59合成fixture；保存旧14业务表排序行值，再迁移并比较完全相等（读取前后不包含新runtime元数据）。
```python
@pytest.mark.parametrize('version', [58, 59])
def test_original_rows_survive(version, tmp_path):
    source = Path('tests/fixtures') / ('legacy_v58.sql' if version == 58 else 'python_v59.sql')
    path = tmp_path/'flexify.sqlite'
    with sqlite3.connect(path) as db:
        db.executescript(source.read_text(encoding='utf-8'))
        before = db.execute('SELECT * FROM training_set_results ORDER BY id').fetchall()
    initialize(path, tmp_path/'backups')
    with sqlite3.connect(path) as db:
        assert db.execute('SELECT * FROM training_set_results ORDER BY id').fetchall() == before
```
- [ ] 先确认新用例揭示未适配的旧状态/恢复版本拒绝，再最小修改版本适配；若测试直接通过，记录覆盖新增而非人为破坏制造红灯。
- [ ] 备份恢复测试损坏文件、未知版本、恢复中异常、活动训练拒绝恢复；原数据库仍可打开。恢复58/59后可自由训练，导出再恢复60结果一致。
- [ ] 运行全部pytest。删除/改写旧UI专属断言时在报告解释产品规则变化；不删除原迁移、备份和数据完整性用例以获得绿灯。
- [ ] 写 `docs/v2.2-validation.md` 记录测试数量、实际结果及未覆盖设备。提交 `test: verify dart and python upgrade compatibility`。

### Task 8: 构建、隔离模拟器升级和交付

**Files:** 修改 `scripts/build-app.ps1`、`scripts/toolchain.ps1`、`scripts/verify-release.py`；新增 `deliverables/安装与旧数据继承.md`、更新验证报告。
**Interfaces:** build-app.ps1 -BuildNumber 40709；复用verify-release.py APK身份/依赖验证，增加与v2.1同ABI版本比较。输出arm64-v8a和x86_64 APK及SHA256、大小、versionCode、签名摘要。

- [ ] 核查脚本所有可写路径在v2.2；只读引用旧Android/Flutter/签名工具，缺Java时在v2.2缓存建立显式配置，不偷偷依赖旧venv。先核查已有版本高水位；若40709不足，自动选更高build number并记录。
- [ ] 保持双ABI一次构建，避免先前x86单独构建导致Python依赖缺失：
```powershell
.\scripts\build-app.ps1 -BuildNumber 40709
```
预期exit0且两份APK存在；失败时留日志，不把旧APK复制成新交付。
- [ ] 用aapt/apksigner及现有verify-release校验包名、两份versionCode、同原签名、Python依赖和实际大小；新增低versionCode候选拒绝测试，不能仅比较Dart旧APK。
- [ ] 在独立UpgradeProbe模拟器分别测试Dart→v2.2、Python v2.1→v2.2覆盖安装；安装前合成记录、暂停草稿，安装后对比并继续训练。只操作明确serial，不操作用户已有5554模拟器，不卸载真实手机旧版。
- [ ] 在同模拟器验证首开记录、四按钮、单项保存、取消、返回键、暂停/重启、左右日期、今日体重、体重图、备份恢复。构建期间不同时运行重型模拟器以控制内存。
- [ ] 将通过检查的产物复制至deliverables，生成哈希与安装指南：手机先旧版导出备份，直接覆盖安装；不能覆盖时停止并核查签名/版本，不能建议先卸载；备份恢复是替换不是合并。实体机未测明确写明。
- [ ] 提交脚本与验证文档；APK和数据库不进Git。最终说明修改范围、自动化/模拟器结果、包路径、大小和未验证事项；未通过项不得宣称通过。

## 自审与实施顺序

规格1—8节均已映射：页面6、输入2/3、时间4、兼容1/4/7/8、图与统计5/6、验收7/8。确认五项Review Focus各有对应测试。计划中的新接口均在公共约定或所属任务定义；后续实现可保持旧接口的兼容包装，不另起应用架构。

建议由当前会话按顺序实施，结束后安排一次独立审查；无需每个任务都新建代理。当前仅完成设计和计划，尚未复制产品代码、创建虚拟环境或生成v2.2 APK。
